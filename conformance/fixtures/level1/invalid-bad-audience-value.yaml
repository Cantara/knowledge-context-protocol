# Level 1 — unit has an unrecognised audience value
# Note: unknown audience values produce warnings, not errors, per spec.
# This fixture is "valid" (no errors) but should produce warnings.
kcp_version: "0.10"
project: test-bad-audience
version: "1.0.0"

units:
  - id: bad-audience
    path: docs/guide.md
    intent: "A guide"
    scope: global
    audience: [alien]
