# Level 1 — unit missing required 'id' field
kcp_version: "0.10"
project: test-missing-id
version: "1.0.0"

units:
  - path: docs/guide.md
    intent: "A guide without an id"
    scope: global
    audience: [human]
