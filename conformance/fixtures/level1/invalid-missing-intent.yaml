# Level 1 — unit missing required 'intent' field
kcp_version: "0.9"
project: test-missing-intent
version: "1.0.0"

units:
  - id: no-intent
    path: docs/guide.md
    scope: global
    audience: [human]
