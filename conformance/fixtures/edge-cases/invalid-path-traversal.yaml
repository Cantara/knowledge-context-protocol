# Edge case — path traversal attack (§12: must reject)
kcp_version: "0.7"
project: test-path-traversal
version: "1.0.0"

units:
  - id: evil-unit
    path: "../../etc/passwd"
    intent: "Attempt to escape manifest root"
    scope: global
    audience: [agent]
