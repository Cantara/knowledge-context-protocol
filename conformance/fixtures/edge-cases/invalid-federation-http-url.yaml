# Edge case — manifest URL uses http:// instead of required https://
# All validators must reject this with an error (§3.6: URL MUST use HTTPS)
kcp_version: "0.9"
project: test-federation-http-url
version: "1.0.0"

manifests:
  - id: platform
    url: "http://example.com/platform/knowledge.yaml"
    label: "Platform Team"
    relationship: foundation

units:
  - id: overview
    path: docs/overview.md
    intent: "Project overview"
    scope: global
    audience: [developer, agent]

relationships: []
