# Edge case — absolute path in unit (§12: must reject)
kcp_version: "0.10"
project: test-absolute-path
version: "1.0.0"

units:
  - id: abs-path-unit
    path: "/etc/shadow"
    intent: "Attempt to use absolute path"
    scope: global
    audience: [agent]
