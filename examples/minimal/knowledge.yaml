# Minimal KCP example — five minutes to add.
# This is a valid knowledge.yaml at Level 1 conformance.
# Add more fields (validated, depends_on, triggers) as your needs grow.

kcp_version: "0.2"
project: my-project
version: 1.0.0

units:
  - id: overview
    path: README.md
    intent: "What is this project and how do I get started?"
    scope: global
    audience: [human, agent]
